// Example code
describe('TagService test!', () => {
  it('create', () => {
    expect(1 + 2).toEqual(3);
  });
  it('get one', () => {
    expect(1 + 2).toEqual(3);
  });
  it('update', () => {
    expect(1 + 2).toEqual(3);
  });
  it('get all', () => {
    expect(1 + 2).toEqual(3);
  });
  it('delete', () => {
    expect(1 + 2).toEqual(3);
  });
});
